use std::str::FromStr;

grammar;

pub Queries: Vec<Query> = {
    let mut queries = Vec::new();
    while !eof() {
        queries.push(query());
    }
    queries
};

Identifier: str = r"[a-zA-Z_][a-zA-Z0-9_\-]*";
Numberic: i32 = <s:r"[0-9]+"> => i32::from_str(s).unwrap();
